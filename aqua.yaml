---
# aqua - Declarative CLI Version Manager
# https://aquaproj.github.io/
#
# インフラ用の CLI の版をここで固定する。devcontainer と CI で同じ版を使う。
# 実体は初回実行時に落ちる (aqua i -l でリンクだけ作ってある)。
# 版を上げたら aqua upc -prune で aqua-checksums.json を更新する
checksum:
  enabled: true
  require_checksum: true
registries:
  - type: standard
    ref: v4.566.0 # renovate: depName=aquaproj/aqua-registry
packages:
  - name: hashicorp/terraform@v1.16.4
  - name: terraform-linters/tflint@v0.64.0
  - name: kayac/ecspresso@v2.8.6
  - name: aws/aws-cli@2.37.1
