# https://www.playframework.com/documentation/latest/Configuration

# 自サービスのホスト名。この host 宛の URL は自己参照（リダイレクトループ）として拒否する。
# ポートは含めない（java.net.URI#getHost と比較するため）。
shortener.host = "localhost"
shortener.host = ${?SHORTENER_HOST}
