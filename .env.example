# compose passes this to server via env_file. Use it with cp .env.example .env.
# Public base URL for short URLs shown to users. If unset, the server default (http://localhost:5173) is used.
SHORTENER_BASE_URL=https://example.com/
