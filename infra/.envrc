# AWS auth used under infra/. All 3 environments are in the same account, so there is one profile
export AWS_PROFILE=short-link-develop
export AWS_REGION=ap-northeast-1
source_env_if_exists .envrc.local
