desc: Server is up and returns JSON
labels:
  - smoke
runners:
  req: ${E2E_BASE_URL:-http://localhost:9000}
vars:
  # Service token that passes Cloudflare Access (develop / staging). make e2e-remote passes it from SSM.
  # Locally (make e2e) it is sent empty and Vite / Play ignore it. Used in the headers of every step
  access: &access
    CF-Access-Client-Id: "${CF_ACCESS_CLIENT_ID:-}"
    CF-Access-Client-Secret: "${CF_ACCESS_CLIENT_SECRET:-}"
steps:
  health:
    # Through the front, GET / returns index.html, so check the health check on the /api side
    desc: GET /api/v1/health returns ok
    req:
      /api/v1/health:
        get:
          headers:
            <<: *access
            Accept: application/json
    test: |
      current.res.status == 200
      && current.res.body.status == 'ok'
